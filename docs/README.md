# Docs

Notes, API documentation, and architecture writeups for the frontend/backend toolkit split live here.

Suggested future docs:

- `architecture.md` for the project/artifact/job model
- `api-contract.md` for the FastAPI resource and job contract
- `fixtures.md` for the placeholder backend data model
